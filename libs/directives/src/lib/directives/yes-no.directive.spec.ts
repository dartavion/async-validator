import { YesNoDirective } from './yes-no.directive';

describe('YesNoDirective', () => {
  it('should create an instance', () => {
    const directive = new YesNoDirective();
    expect(directive).toBeTruthy();
  });
});
